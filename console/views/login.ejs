<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexus | Authorized Access Required</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --primary: #472C8C;
            --primary-light: #5d34a4;
            --bg: #F4F6F9;
            --text-main: #1D1D21;
            --text-muted: #8E92BC;
            --card-bg: #FFFFFF;
            --border: #EDF0F5;
            --danger: #ef4444;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Outfit', sans-serif;
        }

        body {
            background: var(--bg);
            color: var(--text-main);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden;
        }

        .login-container {
            width: 100%;
            max-width: 440px;
            padding: 20px;
        }

        .login-card {
            background: white;
            padding: 50px;
            border-radius: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.04);
            border: 1px solid rgba(255, 255, 255, 0.8);
            position: relative;
            overflow: hidden;
        }

        .logo-area {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 40px;
        }

        .logo-sq {
            width: 48px;
            height: 48px;
            background: var(--primary);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 900;
            font-size: 24px;
            margin-bottom: 16px;
            box-shadow: 0 8px 16px rgba(71, 44, 140, 0.2);
        }

        .logo-text {
            font-size: 24px;
            font-weight: 800;
            color: var(--text-main);
            letter-spacing: -0.5px;
        }

        .login-title {
            text-align: center;
            font-size: 15px;
            font-weight: 500;
            color: var(--text-muted);
            margin-bottom: 32px;
        }

        .form-group {
            margin-bottom: 24px;
        }

        label {
            display: block;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-main);
            margin-bottom: 10px;
            padding-left: 4px;
        }

        input {
            width: 100%;
            padding: 14px 18px;
            border: 1px solid var(--border);
            border-radius: 14px;
            font-size: 15px;
            font-weight: 500;
            outline: none;
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            background: #F9FAFB;
        }

        input:focus {
            border-color: var(--primary);
            background: white;
            box-shadow: 0 0 0 4px rgba(71, 44, 140, 0.05);
        }

        .btn-login {
            width: 100%;
            padding: 16px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 14px;
            font-size: 15px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 8px;
            box-shadow: 0 8px 20px rgba(71, 44, 140, 0.15);
        }

        .btn-login:hover {
            background: var(--primary-light);
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(71, 44, 140, 0.2);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .error-banner {
            background: #FEE2E2;
            color: var(--danger);
            padding: 14px;
            border-radius: 12px;
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 24px;
            text-align: center;
            border: 1px solid #FECACA;
            animation: shake 0.4s cubic-bezier(.36, .07, .19, .97) both;
        }

        @keyframes shake {

            10%,
            90% {
                transform: translate3d(-1px, 0, 0);
            }

            20%,
            80% {
                transform: translate3d(2px, 0, 0);
            }

            30%,
            50%,
            70% {
                transform: translate3d(-4px, 0, 0);
            }

            40%,
            60% {
                transform: translate3d(4px, 0, 0);
            }
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: var(--text-muted);
            font-weight: 500;
        }

        /* Ambient background blobs for premium feel */
        .blob {
            position: fixed;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(71, 44, 140, 0.03) 0%, rgba(255, 255, 255, 0) 70%);
            z-index: -1;
            border-radius: 50%;
        }

        .blob-1 {
            top: -100px;
            right: -100px;
        }

        .blob-2 {
            bottom: -100px;
            left: -100px;
        }
    </style>
</head>

<body>
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>

    <div class="login-container">
        <div class="login-card">
            <div class="logo-area" style="margin-bottom: 24px;">
                <img src="/nexus-admin/logo-login.png" alt="Nexus Brand"
                    style="height: 80px; width: auto; filter: drop-shadow(0 4px 10px rgba(0,0,0,0.05));">
            </div>

            <div class="login-title">Authorized Personnel Only</div>

            <% if (error) { %>
                <div class="error-banner">
                    Authentication Failed: Invalid Credentials
                </div>
                <% } %>

                    <form action="/nexus-admin/login" method="POST">
                        <div class="form-group">
                            <label for="username">Administrator</label>
                            <input type="text" name="username" id="username" placeholder="Enter identifier" required
                                autocomplete="username">
                        </div>
                        <div class="form-group" style="margin-bottom: 32px;">
                            <label for="password">Security Key</label>
                            <input type="password" name="password" id="password" placeholder="••••••••" required
                                autocomplete="current-password">
                        </div>
                        <button type="submit" class="btn-login">Authorize Access</button>
                    </form>
        </div>
        <div class="footer">
            Nexus • Enterprise Shield
        </div>
    </div>
</body>

</html>